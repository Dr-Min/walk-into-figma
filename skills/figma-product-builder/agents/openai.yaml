interface:
  display_name: "Figma Product Builder"
  short_description: "Build structured product screens and prototypes in Figma"
  default_prompt: "Use $figma-product-builder to build the approved product design as a structured, reusable, clickable Figma file."
policy:
  allow_implicit_invocation: false
